import { PublicPlaceholderPage } from './PublicPlaceholderPage';

export function ProjectDetailPage() {
  return (
    <PublicPlaceholderPage
      title="Project Detail"
      description="Project detail pages will use backend project slug or identifier later."
    />
  );
}
