import { PublicPlaceholderPage } from './PublicPlaceholderPage';

export function CertificatesPage() {
  return (
    <PublicPlaceholderPage
      title="Certificates"
      description="Published certificates will be fetched from the backend later."
    />
  );
}
